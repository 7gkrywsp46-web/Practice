n, m = map(int, input().split())

edges = []
connected = set()

for _ in range(m):
    u, v = map(int, input().split())
    edges.append((u, v))
    connected.add(u)
    connected.add(v)


isolated = n - len(connected)
print(isolated)
